---
name: ozon-reviews
description: Работа с отзывами покупателей на товары Ozon. Получение списка отзывов, фильтрация по рейтингу/дате, чтение комментариев, ответы на отзывы, обновление статуса отзывов. Использовать когда нужно мониторить, анализировать или отвечать на отзывы покупателей.
---

# Ozon Reviews Workflow

Работа с отзывами покупателей через Ozon Seller API.

## ⚠️ Обязательная настройка перед использованием

**Перед первым использованием скилла необходимо настроить доступ к API Ozon!**

### Шаг 1: Получение API ключей Ozon

Чтобы работать с API Ozon, вам нужно получить **Client ID** и **API Key** в личном кабинете продавца:

1. **Войдите в личный кабинет Ozon Seller:**
   - Перейдите на https://seller.ozon.ru/
   - Авторизуйтесь с вашими учетными данными

2. **Перейдите в настройки API:**
   - В левом меню выберите **"Настройки"** → **"API ключи"**
   - Или перейдите напрямую: https://seller.ozon.ru/app/settings/api-keys

3. **Создайте новый API ключ:**
   - Нажмите кнопку **"Создать ключ"**
   - Выберите тип ключа: **"Seller API"**
   - Укажите название ключа (например: "Reviews Bot")
   - Скопируйте сгенерированный **API Key**
   - Скопируйте **Client ID** (отображается вверху страницы)

4. **Настройте права доступа:**
   - Убедитесь, что у ключа есть права на:
     - `Чтение отзывов` (review/list)
     - `Отправка ответов` (review/comment/create)
     - `Изменение статуса отзывов` (review/change-status)

> ⚠️ **Важно:** API Key отображается только один раз при создании! Сохраните его в надежном месте.

### Шаг 2: Настройка скилла

Создайте файл `.env` в директории скилла (`skills/ozon-reviews-workflow/.env`):

```bash
# Ozon API Credentials
OZON_CLIENT_ID=ваш_client_id_здесь
OZON_API_KEY=ваш_api_key_здесь
```

**Или** создайте `.env` в рабочей директории (workspace), где будете запускать команды.

### Шаг 3: Проверка настройки

Проверьте, что всё работает:

```bash
python3 scripts/reviews.py --limit 5
```

Если настройка выполнена правильно, вы увидите список последних отзывов. Если нет — будет сообщение об ошибке авторизации.

---

## Использование

```bash
# Получить последние отзывы
python3 scripts/reviews.py

# Фильтр по рейтингу (только 1-2 звезды)
python3 scripts/reviews.py --rating-min 1 --rating-max 2

# Ответить на отзыв
python3 scripts/reviews.py --reply-to ID --text "Благодарим!"

# Получить комментарии к отзыву
python3 scripts/reviews.py --comments-for ID

# Обновить статус отзывов (ОБЯЗАТЕЛЬНО после ответа!)
python3 scripts/mark_processed.py
```

## ⚠️ ВАЖНО: Обновление статуса отзывов

**После отправки ответа на отзыв ОБЯЗАТЕЛЬНО нужно обновить его статус на `PROCESSED`!**

### Почему это важно:

- Без обновления статуса отзыв остаётся `UNPROCESSED` даже с комментарием
- Это мешает фильтрации и аналитике
- В кабинете продавца отзыв будет отображаться как "без ответа"

### Как обновить статус:

```bash
# Показать отзывы, требующие обновления статуса
python3 scripts/mark_processed.py --dry-run

# Обновить все отзывы с комментариями на PROCESSED
python3 scripts/mark_processed.py

# Обновить без подтверждения
python3 scripts/mark_processed.py --yes
```

### В рабочем процессе:

```bash
# 1. Ответить на отзывы
python3 scripts/reviews.py --reply-to ID --text "Спасибо!"

# 2. Обязательно обновить статус!
python3 scripts/mark_processed.py
```

## API Методы

### 1. Список отзывов
```bash
POST /v1/review/list
```

**Параметры:**
- `limit`: 20-100 (обязательно)
- `sort_dir`: "ASC" или "DESC" (по дате)
- `sku`: фильтр по артикулу (опционально)

**Ответ:**
```json
{
  "reviews": [
    {
      "id": "017c0ddf-...",
      "sku": 181649408,
      "text": "Отличный товар!",
      "rating": 5,
      "status": "UNPROCESSED",
      "published_at": "2026-02-02T10:00:00Z",
      "comments_amount": 0,
      "photos_amount": 2,
      "videos_amount": 0
    }
  ],
  "has_next": true,
  "last_id": "..."
}
```

### 2. Комментарии к отзыву
```bash
POST /v1/review/comment/list
```

**Параметры:**
- `review_id`: UUID отзыва
- `limit`: 20-100

### 3. Ответить на отзыв
```bash
POST /v1/review/comment/create
```

**Параметры:**
- `review_id`: UUID отзыва
- `text`: текст ответа

**Ответ:**
```json
{"comment_id": "019c1f2c-..."}
```

### 4. Обновить статус отзыва ⚠️
```bash
POST /v1/review/change-status
```

**Параметры:**
- `review_ids`: массив UUID отзывов (1-100 штук)
- `status`: "PROCESSED" или "UNPROCESSED"

**Пример:**
```json
{
  "review_ids": [
    "017c0ddf-8b43-854b-4b67-75676025a1c1",
    "017c0de6-3b86-8e7f-4f81-6bf2147fa38b"
  ],
  "status": "PROCESSED"
}
```

## Статусы отзывов

| Статус | Описание | Действие |
|--------|----------|----------|
| `UNPROCESSED` | Новый отзыв, без ответа | Нужно ответить |
| `UNPROCESSED` + комментарий | Есть ответ, но статус не обновлён | ⚠️ **Обновить статус!** |
| `PROCESSED` | Есть ответ, статус обновлён | ✅ Готово |

## Примеры

**Полный рабочий процесс:**
```python
from ozon_reviews import ReviewsClient

client = ReviewsClient()

# 1. Получить неотвеченные отзывы
reviews = client.get_reviews(status="UNPROCESSED")

# 2. Ответить на отзыв
for r in reviews:
    result = client.reply(r['id'], "Благодарим за отзыв!")
    print(f"Reply sent: {result['comment_id']}")

# 3. ⚠️ ОБЯЗАТЕЛЬНО обновить статус
review_ids = [r['id'] for r in reviews]
client.change_status(review_ids, "PROCESSED")
print("Status updated to PROCESSED")
```

**Обновить статус после массовой рассылки:**
```bash
# Найти все UNPROCESSED с комментариями и обновить
python3 scripts/mark_processed.py
```

## 🤖 AI-генерация ответов

### Workflow с AI:

```bash
# 1. Автоответы на 5★ без текста (шаблоны)
python3 scripts/autoreply.py --limit 100

# 2. Экспорт отзывов 4-5★ с текстом для AI
python3 scripts/ai_generator.py --limit 20

# 3. AI генерирует персонализированные ответы
# (передай файл OpenClaw с правилами company-policy.md)

# 4. Импорт AI-ответов
python3 scripts/ai_reply.py --import-file ai_reviews_*_replied.json
```

### Правила компании (company-policy.md):

- ❌ **Никаких возвратов/компенсаций** после приемки товара
- ✅ Направляй в Ozon по вопросам логистики/упаковки
- ✅ Предлагай консультацию по применению продукта
- ✅ Эмоциональная поддержка без финансовых обязательств

## Скрипты

- `reviews.py` — получить список, ответить на отзывы
- `mark_processed.py` — обновить статус на PROCESSED ⚠️ **ОБЯЗАТЕЛЬНО**
- `get_comments.py` — комментарии к отзыву
- `autoreply.py` — автоответы на 5★ без текста
- `ai_generator.py` — экспорт для AI-генерации
- `ai_reply.py` — импорт AI-ответов

См. [references/ozon-reviews-api.md](references/ozon-reviews-api.md) для деталей API.
См. [references/company-policy.md](references/company-policy.md) для правил компании.

## Полезные ссылки

- [Ozon Seller API Documentation](https://docs.ozon.ru/api/seller/)
- [Ozon Global API Docs](https://docs.ozon.ru/global/en/api/intro/)
- [Личный кабинет Ozon Seller](https://seller.ozon.ru/)
